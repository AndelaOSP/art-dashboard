const filters = [
  { asset_types: ['Headsets'] },
  { model_numbers: ['HP 27ES'] }
];

export const selectedFilters = {
  asset_types: ['Headsets'],
  model_numbers: ['HP 27ES']
};

export default filters;
